services:
  database:
    image: postgres:latest
    container_name: rupee-database
    environment:
      POSTGRES_DB: rupee
      POSTGRES_USER: rupee-user
      POSTGRES_PASSWORD: rupee-password
    ports:
      - 5432:5432
    volumes:
      - database_data:/var/lib/postgresql

volumes:
  database_data:
